<%= form_for(@changing) do |f| %>
  <% if @changing.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@changing.errors.count, "error") %> prohibited this changing from being saved:</h2>

      <ul>
      <% @changing.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <p><%= f.label :pee %><%= f.check_box :pee %>
  <p><%= f.label :poo %><%= f.check_box :poo %>
  <p><%= f.label :time %><%= f.text_field :time %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
